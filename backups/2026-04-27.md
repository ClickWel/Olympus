# Session 2026-04-27 — Hermes-Desktop Infrastructure

## Tasks Completed

- Deleted Mac/WSL-only skill folders from shared skills dir (apple/, etc.)
- Wired Talos into Discord: trusted_bot_ids, allow_all_users, require_mention, reactions
- Fixed Talos SOUL.md (was "Researcher" template, replaced with real D:\Talos\SOUL.md)
- Fixed Ares SOUL.md (was "Startup CTO" template, replaced with D:\Ares\AGENTS.md)
- Set Talos default model to qwen3.5-9b-glm5.1-distill-v1-i1 with full local alias set:
  - i1 (default, 140501 ctx), omniclaw (125049), opus (140501), coder (140501), neo (140501), qwen (102400)
- Added model aliases to Hermes and Ares profiles: minimax, minimax-clod, sonnet, opus, free
- Fixed collab ping loop bug: bots now check D:\Shared\collab\session.lock before firing ping
- Published HermesDesktop.exe as self-contained to D:\HW\publish\
- Created VBS launchers (launch-hermes.vbs, launch-ares.vbs, launch-talos.vbs) - no console window
- Fixed Enter/Shift+Enter: AcceptsReturn=False, KeyDown inserts \r\n for Shift+Enter
- Enabled reactions in all three Discord profiles
- Added image drop/paste vision support to ChatPage:
  - DragOver, Drop, Paste handlers on input area
  - ImagePreviewPanel thumbnail with X button
  - SendVisionRequestAsync: isolated direct HttpClient to OpenRouter, never touches factory
  - Fixed multimodal content double-serialization in OpenAiClient.BuildPayload (detect '[' prefix, deserialize as JsonElement)
  - Final working model: nvidia/nemotron-nano-12b-v2-vl:free
  - Timeout: 60s, max_tokens: 512

## Decisions

- VBS launchers chosen over bat files to eliminate console window
- Vision call completely isolated from ChatClientFactory to prevent model contamination on failure
- Free tier vision model is slow by nature (queue time) - not fixable without paid tier

## Open Items

1. Rebuild/publish pending - app was running when session ended, close all three bots first
2. STT (voice to text, F9 hotkey) - deferred to next session
3. Three-way collab test with Talos (Talos added late, full test not done)
4. "Message Hermes..." placeholder hardcoded - should reflect profile name on load
5. Monitor Talos flashing terminal window (likely LM Studio inference workers, not HW)

## Follow-up Session (same day)

### Tasks Completed
- Swapped vision model from nvidia/nemotron-nano-12b-v2-vl:free to openrouter/free
- openrouter/free smart-filters to vision-capable models automatically -- no hardcoded list needed
- Published updated exe
- Confirmed working: vision response in seconds vs previous 30-60s

### Decisions
- openrouter/free is the correct router for vision -- handles feature detection automatically
- openrouter/auto is configured for text models only on the dashboard, not suitable for vision

### Open Items (updated)
1. "Message Hermes..." placeholder hardcoded -- should reflect profile name
2. STT / voice input (F9) -- not started
3. Three-way collab test -- not done
4. Talos flashing terminal -- cosmetic

## Morning Session (2026-04-27)

### Tasks Completed
- Diagnosed intermittent vision 404: openrouter/free occasionally routes to Nvidia nemotron which returns 404 on provider side
- Added silent retry logic to SendVisionRequestAsync -- retries once on any 4xx, router picks a different model
- Replaced raw JSON error dump with clean user-facing message if both attempts fail
- Published updated exe

### Decisions
- Root cause is provider-side 404 from bad router selection, not a warm-up issue
- One silent retry is sufficient -- free router rarely picks the same bad model twice

### Open Items (current)
1. "Message Hermes..." placeholder hardcoded -- should reflect profile name
2. STT / voice input (F9) -- not started
3. Three-way collab test -- not done
4. Talos flashing terminal -- cosmetic

## Token estimate: ~300k + ~50k + ~30k (morning)

## Afternoon Session (2026-04-27) — Hermes Windows Migration

### Tasks Completed
- Audited all WSL-era broken paths across Hermes profiles and skills
- Fixed SOUL.md files:
  - ares/SOUL.md: removed dead sky-memory/Ollama block, fixed skill_framework path
  - talos/SOUL.md: fixed fdfind path from /mnt/d to D:/
- Fixed 7 hermes-cs skill files:
  - brave_cdp.md: 192.168.16.1 -> 127.0.0.1, portproxy section marked not needed
  - browser/cdp-browser-connect/SKILL.md: IPs, Node ws path, script paths, portproxy section
  - browser/browser-otp-bypass/SKILL.md: WIN_HOST fixed, WSL note removed
  - agent-control/skill-enforcement-framework/SKILL.md: CDP address and file paths
  - ares-core-toolkit/SKILL.md: CDP address
  - autonomous-ai-agents/hermes-agent/SKILL.md: path conversion section flipped from /mnt/ to Windows native
- Fixed 5 actual scripts in D:/Ares/scripts/ (caught by Atlas):
  - cdp_runner.js: WIN_HOST, comment
  - cdp_idor.js: FINDINGS_DIR /mnt/d -> D:/
  - check_browsers.py, debug_cdp.py, test_dual_browser.py: all IPs
- Fixed HermesDesktop settings UI bug (profile_name/accent_color wiped on save):
  - Root cause: WriteYamlSectionAsync in HermesEnvironment.cs replaced entire section
  - Fix: read-merge-write -- preserves unknown fields, overlays only changed keys
  - Built and deployed to D:/HW/publish/
- Added then removed VBS patch-profile.py workaround (superseded by source fix)
- Labelled patch-profile.py as retired utility with context for future reference
- Restored Ares display block after config edit stripped it

### Decisions
- Python on PATH (two versions), used for patch script
- Core fix preferred over launcher workaround -- cleaner, no Python dependency on launch
- patch-profile.py kept as retired fallback, not called by anything

### Open Items (carried forward)
1. "Message Hermes..." placeholder hardcoded -- should reflect profile name
2. STT / voice input (F9) -- not started
3. Three-way collab test -- not done
4. Talos flashing terminal -- cosmetic

## Token estimate: ~300k + ~50k + ~30k (morning) + ~80k (afternoon)
