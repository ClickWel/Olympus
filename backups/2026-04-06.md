# Session 2026-04-06

## Tasks Completed
- OpenCode Desktop model lineup decided: Gemma 4 31B (primary), GPT-5 Nano (backup), Gemini 3.1 Flash Lite, Llama 4 Scout
- Removed dead models from OpenCode: Qwen3 32B, Qwen3.6, GPT OSS 20B, ALLaM-2-7b
- Notion MCP added to Clawdbot (Argus only) with Windows cmd /c wrapper fix
- Notion MCP removed from Olympus and OpenCode configs - Argus-only by design
- Talos daily driver testing complete - glm46v-dd declared WINNER
- Talos launcher rebuilt: 6 slots, GLM as daily driver, gemma4-talos removed
- GLM modelfile backed up at D:\Talos\daily-driver\modelfiles\glm46v-dd.Modelfile
- Olympus CLAUDE.md patched with 3 response rules (ship/narrate, zero preamble, deliverable-only logging)
- SessionEnd hook added to CC - auto-kills orphaned node processes older than 1 hour
- Notion MCP /doctor warning fixed - was leaking into Talos daily-driver project config

## Decisions
- Notion MCP = Argus only. OpenCode and Talos excluded to prevent accidental Notion writes.
- glm46v-dd is Talos daily driver. Vision, tools, role switching all passed clean sweep.
- OpenCode Desktop: Gemma 4 31B primary, GPT-5 Nano backup.
- Cleanup.lnk kept for manual VRAM flush, node cleanup now automated.

## Next Steps
- Kiteworks IDOR testing (Atlas + Talos)
- mobilevikings token auto-refresh still pending

---

## Session 2 (Evening) - Bug Bounty Setup + GLM Tool Fix

### Tasks Completed
- Identified bug bounty worker model: `qwen3.5-16k:latest` (confirmed from screenshot, 732k ctx, 6.6 GB)
- Confirmed bug bounty docs intact at `D:\Atlas\projects\bug-bounty\`
- Fixed `coach_loop.py`: MODEL `gemma4-nothink` -> `glm46v-dd`, SCRIPT_GEN_MODEL `qwen3.5:latest` -> `qwen3.5-16k:latest`
- Fixed `worker_loop.py`: MODEL `qwen3.5:latest` -> `qwen3.5-16k:latest`
- Added `[7] qwen3.5-16k` to `D:\Talos\start-talos.bat` as bug bounty worker specialist
- Built Talos memory system: protocol added to CLAUDE.md, MEMORY.md index updated, `tool_call_format.md` lesson written
- Backed up `glm46v-dd.Modelfile` to `.bak`
- Investigated GLM tool call rendering issue via research agent

### Key Finding - GLM Tool Calls
- `glm46v-dd` Modelfile custom TEMPLATE outputs `<tool_call>` XML, CC expects `<tool_use>` with `id` + `input`
- Ollama 0.20.2 is new enough to handle conversion natively - but the custom TEMPLATE overrides it
- Tuftwit correctly called it: "Custom TEMPLATE overriding native conversion. That's the culprit."
- Recommended fix: pull `glm4.7-flash` fresh (no custom template), add as `[8]` in Talos selector, test tools
- Do NOT modify `glm46v-dd.Modelfile` - it's working as coach via API

### Token Count - Why 218k
- Multiple large full-file reads (worker_loop.py alone was 14k+ tokens)
- Two background subagent spawns
- Large Glob trees returned in full
- Screenshots in context carry token weight
- Iterative shell debugging on the .lnk shortcut (5+ failed attempts before writing a .ps1 file)
- Next agent: use Grep/offset before reading full files, write shell scripts instead of inline powershell

### Next Steps
1. Pull `glm4.7-flash` via Ollama, add to Talos selector as `[8]`
2. Test tool execution with 4.7-flash in a Talos CC window
3. If tools work, update Talos CLAUDE.md primary model reference
4. Start bug bounty - last state: Kinepolis/Landmark Cinemas, Chargebee GUID IDOR hot, MovieCard ID 8189 sequential enumeration approach (see `D:\Atlas\projects\bug-bounty\logs\coach_state_backup.md`)
