# Session 2026-04-28

## Tasks Completed

- **Model selector dropdown** - Was only showing the current model. Root cause: `PopulateModelSwitcher` read from a `provider_keys` config section that never existed. Fixed to parse `model_aliases` block from config.yaml via new `ReadModelAliases()` method in `HermesEnvironment.cs`. Duplicate entry fix: skip aliases whose name OR model ID matches the current active model.
- **Talos default model** - Changed from raw model ID `qwen3.5-9b-glm5.1-distill-v1-i1` to alias `i1` for consistency with other aliases.
- **Security classifier (Issue 1)** - Added `ReadOnlyCommands` whitelist to `TokenizedCommandValidator`. Read-only commands (find, grep, ls, cat, etc.) now bypass all security checks and return Safe immediately.
- **NormalizePath %VAR% expansion (Issue 2)** - Added then REVERTED. `Environment.ExpandEnvironmentVariables` was added to `NormalizePath` in `DiffHelper.cs`. Caused Hermes to crash with `KeyNotFoundException` after write_file. Reverted. Root cause of crash not fully identified - deferred.

## Open Items

- **Hermes broken state** - After the NormalizePath revert, Hermes still shows "Error: The given key was not present in the dictionary" on every message. Likely a corrupted message history from a truncated JSON tool call (log shows "Expected depth to be zero" JsonException). Fix: hit New Chat to clear history.
- **Issue 2 (`%USERPROFILE%` expansion)** - Still unimplemented. Need stack trace before reimplementing. Log file: `C:\Users\click\AppData\Local\hermes\profiles\hermes\hermes-cs\logs\hermes.log`.

## Decisions

- Publish workflow: always use `dotnet publish` to `D:\HW\publish\` - that's what the VBS shortcuts point to. `dotnet run` and bin/Release are for dev only.
- Hermes must be closed before publish or DLL lock will fail.

## Evening Session (2026-04-28)

- **MiniMax KeyNotFoundException** - Diagnosed root cause: `AnthropicClient.cs` line 315 used `root.GetProperty("usage")` (hard lookup) inside the `message_delta` SSE handler. MiniMax via OpenRouter sometimes omits the `usage` field entirely, throwing `KeyNotFoundException`. Fixed to `TryGetProperty`. Build and portable publish both clean.

## Late Session (2026-04-28)

- **CompactionManager wired in** - `CompactionManager` was fully built but never called. Wired into `ContextManager`:
  - `CompactMicro` now runs on all messages before window split (strips old tool results, no LLM cost)
  - `SummarizeTextAsync` now handles the eviction summarization path, with 600s cooldown on failure
  - `SanitizeOrphanedToolResults` now runs on `recentTurns` whenever eviction occurred
  - `CompactionOccurred` event added - fires to `ChatPage`, flashes "context compacted" in token footer for 4 seconds
  - Applies to all profiles (Hermes, Talos, Ares)
- Published to `D:\HW\publish\`

## Token Cost

~estimated 180k tokens (morning) + ~15k tokens (evening) + ~25k tokens (late)
