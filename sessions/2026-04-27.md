# Session 2026-04-27 — Hermes-Desktop Infrastructure

## Tasks Completed

- Deleted Mac/WSL-only skill folders from shared skills dir (apple/, etc.)
- Wired Talos into Discord: trusted_bot_ids, allow_all_users, require_mention, reactions
- Fixed Talos SOUL.md (was "Researcher" template, replaced with real D:\Talos\SOUL.md)
- Fixed Ares SOUL.md (was "Startup CTO" template, replaced with D:\Ares\AGENTS.md)
- Set Talos default model to qwen3.5-9b-glm5.1-distill-v1-i1 with full local alias set:
  - i1 (default, 140501 ctx), omniclaw (125049), opus (140501), coder (140501), neo (140501), qwen (102400)
- Added model aliases to Hermes and Ares profiles: minimax, minimax-clod, sonnet, opus, free
- Fixed collab ping loop bug: bots now check D:\Shared\collab\session.lock before firing ping
- Published HermesDesktop.exe as self-contained to D:\HW\publish\
- Created VBS launchers (launch-hermes.vbs, launch-ares.vbs, launch-talos.vbs) - no console window
- Fixed Enter/Shift+Enter: AcceptsReturn=False, KeyDown inserts \r\n for Shift+Enter
- Enabled reactions in all three Discord profiles
- Added image drop/paste vision support to ChatPage:
  - DragOver, Drop, Paste handlers on input area
  - ImagePreviewPanel thumbnail with X button
  - SendVisionRequestAsync: isolated direct HttpClient to OpenRouter, never touches factory
  - Fixed multimodal content double-serialization in OpenAiClient.BuildPayload (detect '[' prefix, deserialize as JsonElement)
  - Final working model: nvidia/nemotron-nano-12b-v2-vl:free
  - Timeout: 60s, max_tokens: 512

## Decisions

- VBS launchers chosen over bat files to eliminate console window
- Vision call completely isolated from ChatClientFactory to prevent model contamination on failure
- Free tier vision model is slow by nature (queue time) - not fixable without paid tier

## Open Items

1. Rebuild/publish pending - app was running when session ended, close all three bots first
2. STT (voice to text, F9 hotkey) - deferred to next session
3. Three-way collab test with Talos (Talos added late, full test not done)
4. "Message Hermes..." placeholder hardcoded - should reflect profile name on load
5. Monitor Talos flashing terminal window (likely LM Studio inference workers, not HW)

## Follow-up Session (same day)

### Tasks Completed
- Swapped vision model from nvidia/nemotron-nano-12b-v2-vl:free to openrouter/free
- openrouter/free smart-filters to vision-capable models automatically -- no hardcoded list needed
- Published updated exe
- Confirmed working: vision response in seconds vs previous 30-60s

### Decisions
- openrouter/free is the correct router for vision -- handles feature detection automatically
- openrouter/auto is configured for text models only on the dashboard, not suitable for vision

### Open Items (updated)
1. "Message Hermes..." placeholder hardcoded -- should reflect profile name
2. STT / voice input (F9) -- not started
3. Three-way collab test -- not done
4. Talos flashing terminal -- cosmetic

## Morning Session (2026-04-27)

### Tasks Completed
- Diagnosed intermittent vision 404: openrouter/free occasionally routes to Nvidia nemotron which returns 404 on provider side
- Added silent retry logic to SendVisionRequestAsync -- retries once on any 4xx, router picks a different model
- Replaced raw JSON error dump with clean user-facing message if both attempts fail
- Published updated exe

### Decisions
- Root cause is provider-side 404 from bad router selection, not a warm-up issue
- One silent retry is sufficient -- free router rarely picks the same bad model twice

### Open Items (current)
1. "Message Hermes..." placeholder hardcoded -- should reflect profile name
2. STT / voice input (F9) -- not started
3. Three-way collab test -- not done
4. Talos flashing terminal -- cosmetic

## Token estimate: ~300k + ~50k + ~30k (morning)

## Afternoon Session (2026-04-27) — Hermes Windows Migration

### Tasks Completed
- Audited all WSL-era broken paths across Hermes profiles and skills
- Fixed SOUL.md files:
  - ares/SOUL.md: removed dead sky-memory/Ollama block, fixed skill_framework path
  - talos/SOUL.md: fixed fdfind path from /mnt/d to D:/
- Fixed 7 hermes-cs skill files:
  - brave_cdp.md: 192.168.16.1 -> 127.0.0.1, portproxy section marked not needed
  - browser/cdp-browser-connect/SKILL.md: IPs, Node ws path, script paths, portproxy section
  - browser/browser-otp-bypass/SKILL.md: WIN_HOST fixed, WSL note removed
  - agent-control/skill-enforcement-framework/SKILL.md: CDP address and file paths
  - ares-core-toolkit/SKILL.md: CDP address
  - autonomous-ai-agents/hermes-agent/SKILL.md: path conversion section flipped from /mnt/ to Windows native
- Fixed 5 actual scripts in D:/Ares/scripts/ (caught by Atlas):
  - cdp_runner.js: WIN_HOST, comment
  - cdp_idor.js: FINDINGS_DIR /mnt/d -> D:/
  - check_browsers.py, debug_cdp.py, test_dual_browser.py: all IPs
- Fixed HermesDesktop settings UI bug (profile_name/accent_color wiped on save):
  - Root cause: WriteYamlSectionAsync in HermesEnvironment.cs replaced entire section
  - Fix: read-merge-write -- preserves unknown fields, overlays only changed keys
  - Built and deployed to D:/HW/publish/
- Added then removed VBS patch-profile.py workaround (superseded by source fix)
- Labelled patch-profile.py as retired utility with context for future reference
- Restored Ares display block after config edit stripped it

### Decisions
- Python on PATH (two versions), used for patch script
- Core fix preferred over launcher workaround -- cleaner, no Python dependency on launch
- patch-profile.py kept as retired fallback, not called by anything

### Open Items (carried forward)
1. "Message Hermes..." placeholder hardcoded -- should reflect profile name
2. STT / voice input (F9) -- not started
3. Three-way collab test -- not done
4. Talos flashing terminal -- cosmetic

## Token estimate: ~300k + ~50k + ~30k (morning) + ~80k (afternoon)

## Evening Session (2026-04-27) — HermesDesktop UI Polish

### Tasks Completed
- Added tool use indicator: `ToolUseStart` events now surface as `ChatStreamEventType.ToolUse`, thinking indicator updates to "Using: [toolname]" in real time during tool calls
- Fixed all hardcoded "Hermes" strings in chat UI - page title, thinking text, input placeholder now pull from `HermesEnvironment.ProfileName` at runtime
- Added per-profile ASCII welcome art: caduceus for Hermes, shield/swords for Ares, hooded figure for Talos
- Multiple rebuild/publish cycles as Jeff iterated on art alignment and trimming
- Confirmed all three profiles launching with correct names and art
- Jeff set OG "neo" Hermes profile working directory to D:\HW (cosmetic)

### Decisions
- Art hardcoded in C# switch rather than loaded from file (keeps startup clean)
- "rebuild" established as shorthand trigger for build+publish

### Open Items (carried forward)
1. STT / voice input (F9) -- not started
2. Three-way collab test -- not done
3. "Hermes Desktop" sidebar label still hardcoded -- should be profile-driven
4. Talos flashing terminal -- cosmetic

## Token estimate: ~300k + ~50k + ~30k (morning) + ~80k (afternoon) + ~120k (evening)

## Late Session (2026-04-27) — HermesDesktop Polish + Talos Fixes

### Tasks Completed
- Tool chain indicator: replaced flashing "Using: X" with persistent colored chain (bash › read_file › terminal) that builds up and holds until response arrives
- Fixed bubble labels: all three profiles now show correct profile name instead of hardcoded "Hermes"
- Fixed working directory revert: now reads/writes from profile config yaml instead of shared env var (isolated per profile)
- Fixed terminal windows stealing focus: TerminalTool.cs CreateNoWindow false -> true
- Tool chain wrapping: switched ThinkingIndicator from fixed-height StackPanel to Grid with TextWrapping so long chains wrap instead of going offscreen
- Fixed profile name in thinking dots: reads HermesEnvironment.ProfileName at runtime, not hardcoded string resource
- Fixed approval timeout: ApprovalTimeoutSeconds now reads agent.approval_timeout with fallback to approvals.timeout - was a dead config key before
- Fixed local model timeout: HttpClient timeout now 30 min for local/LAN models, 5 min for cloud - was killing Talos runs at 5 min
- Talos SOUL.md: added Authorization section - stops model pulling ethical ripcord on legitimate bug bounty targets
- BashTool security policy: AutoApproveWarnings=true - was blocking || pipes, mixed quotes, redirects that Talos needs for bug bounty work

### Decisions
- AutoApproveWarnings only suppresses NeedsReview - Dangerous and TooComplex still hard-stop
- Local model detection by provider name OR base URL (localhost/127.0.0.1/10.0.0.x)
- Tool chain clears on first response token, not on tool completion - keeps last tool visible until answer arrives

### Open Items (carried forward)
1. STT / voice input (F9) -- not started
2. Three-way collab test -- not done
3. "Hermes Desktop" sidebar label still hardcoded
4. Talos flashing terminal -- cosmetic
5. Empty bubbles when model produces tool calls with no text content -- rendering bug
6. NEXT BIG THING: Scrolling chat activity feed (see handoff below)

## Token estimate: ~300k + ~50k + ~30k (morning) + ~80k (afternoon) + ~120k (evening) + ~150k (late)

## Night Session (2026-04-27) — Scrolling Chat Activity Feed + Polish

### Tasks Completed
- Built DataTemplateSelector (ChatFeedTemplateSelector.cs) routing ChatMessageItem to bubble, ActivityDisplayItem to tool card
- Switched MessagesList from ObservableCollection<ChatMessageItem> to ObservableCollection<object>
- Wired ActivityEntryAdded into live chat feed -- Running entries insert inline tool cards, Success/Failed update in place
- Added ItemContainerStyle to fix ListView item height clipping
- Raised InputSummary truncation from 200 to uncapped (full args visible in cards)
- Fixed duplicate Talos bubbles on session reload -- consecutive assistant messages now merged into one bubble
- Wired InsightsService into HermesChatService -- RecordSession, RecordToolUse, RecordTokens now firing
- Added token counter to chat footer: X/Y format with progress bar and % label
- Added compaction threshold label below counter (reads compression.threshold from profile config)
- Removed "Hermes can make mistakes" disclaimer from footer
- Fixed Stop button to immediately reset UI without waiting for HTTP drain
- Set context.window in Ares (204800) and Talos (140502) profile configs

### Decisions
- InputSummary uncapped -- tool results stay at 2000 chars to avoid memory bloat
- Context limit uses context.window config key, falls back to provider-type heuristic (local=128K, cloud=32K)
- Stop button now does immediate UI reset; underlying HTTP request drains in background

### Open Items (carried forward)
1. STT / voice input (F9) -- not started
2. Three-way collab test -- not done
3. "Hermes Desktop" sidebar label still hardcoded
4. Talos flashing terminal -- cosmetic
5. Tasks tab -- shows "No tasks yet" even when agent creates tasks (next session)
6. Files tab -- tree renders but folder/file labels missing (next session)
7. Streaming inline (step 4 of feed plan) -- thinking tokens as live feed items, not started

## Token estimate: ~300k + ~50k + ~30k + ~80k + ~120k + ~150k + ~180k (night)

## Post-Reboot Session (2026-04-27) — HermesDesktop Tab Fixes + Agent UI

### Tasks Completed
- Fixed FileBrowserPanel blank labels: `{Binding Icon/Name}` -> `{Binding Content.Icon/Name}` (WinUI TreeView DataContext is TreeViewNode, not Content)
- Wired TaskPanel to live-update from agent activity via TaskManager.TaskChanged event
- Wired TodoWriteTool to InMemoryTodoStore singleton -- Tasks tab now mirrors agent's todo_write list in real time
- Removed narration rule from Talos SOUL.md
- Added todo_write task tracking instruction to Talos SOUL.md
- Fixed Hermes profile context window: added `window: 204800` to config.yaml (was defaulting to 32K)
- Fixed ghost process on window close: added `Closed += (_, _) => Environment.Exit(0)` to MainWindow
- Agent panel Souls tab: removed "Apply This Soul" (destructive overwrite), replaced with "Merge to Editor"
- Merge to Editor: loads current soul + separator + template so user can cherry-pick sections
- Reset button: now restores from session-start snapshot of real soul, not generic Hermes template

### Decisions
- TodoWriteTool is the right mechanism for live task progress -- models use it naturally, no SOUL forcing needed
- Tasks tab shows todo_write list when populated, falls back to TaskManager when empty
- Souls tab templates are overlays, not replacements -- Agents tab is for saving named blends

### Open Items
- **FIRST THING NEXT SESSION: republish after reboot** -- publish DLLs are still 19:11 build, all changes compiled but not deployed due to stale file lock from ghost process
- STT / voice input (F9) -- not started
- Three-way collab test -- not done
- "Hermes Desktop" sidebar label still hardcoded
- Talos flashing terminal -- cosmetic

## Token estimate: ~300k + ~50k + ~30k + ~80k + ~120k + ~150k + ~180k + ~120k (post-reboot session)

## Final Session (2026-04-27) — Soul Sync + Pending Audit

### Tasks Completed
- Diagnosed Hermes profile SOUL.md: had random "DevOps Engineer" template, not real identity
- Replaced with D:\Hermes\AGENTS.md content (correct soul)
- Synced Talos: profile was missing todo_write task tracking rule, master was missing Authorization section -- merged both, both files now consistent
- Synced Ares: profile had cleaner memory section and Windows-native paths, synced master AGENTS.md from profile
- Confirmed Souls tab still showing old "Apply This Soul" / "Copy to Editor" buttons -- change from previous session not in the running build (needs publish)
- Read Hermes-Desktop GitHub repo docs -- no new architectural changes required

### Decisions
- Soul source of truth: profile SOUL.md is authoritative (agent reads it at runtime), master files in D:\Hermes/Ares/Talos are mirrors
- Souls tab button rename/remove is code-complete in source but never published -- first thing next session

### Open Items
1. **PUBLISH FIRST**: Souls tab still shows "Apply This Soul" in running build -- source is correct, just needs publish
2. todo_write schema fix -- same situation, source correct, not deployed
3. STT / voice input (F9) -- not started
4. Three-way collab test -- not done
5. "Hermes Desktop" sidebar label hardcoded
6. Talos flashing terminal -- cosmetic

## Token estimate: ~300k + ~50k + ~30k + ~80k + ~120k + ~150k + ~180k + ~120k + ~40k (final)

## Post-Final Session (2026-04-27) — STT + Polish

### Tasks Completed
- Fixed Tasks tab not populating: root cause was DispatcherQueue null on background thread in TaskPanel.OnTodosChanged -- captured main dispatcher at construction time
- Added todo_write and checkpoint to readOnlyTools in PermissionManager (no permission prompt for internal state tools)
- Fixed ShellNavigation.PaneTitle hardcoded "Hermes Desktop" -- now set from ProfileName in MainWindow constructor
- Added todo_write task tracking rule to Hermes and Ares profile SOUL.md (Talos already had it): auto-use for 5+ step tasks
- Built STT voice input: F9 toggle + mic button, NAudio capture, Groq Whisper-large-v3-turbo, transcript drops into input box
- Added MASTER_API_KEYS.env loader at app startup (LoadMasterApiKeys in App.xaml.cs) -- GROQ_API_KEY now available without system env var
- All published to D:\HW\publish\

### Decisions
- Kept F9 alongside mic button -- zero complexity cost, useful for keyboard-only flow
- DispatcherQueue captured at construction (UI thread) not at event time (background thread)
- MASTER_API_KEYS.env only sets keys not already in environment (safe, non-destructive)

### Open Items
1. STT untested -- first thing next session, restart bots and press F9 / mic button
2. Three-way collab test -- not done
3. Talos flashing terminal -- cosmetic

## Token estimate: ~300k + ~50k + ~30k + ~80k + ~120k + ~150k + ~180k + ~120k + ~40k + ~80k (post-final)

## Confirmed
- STT voice input working as expected. Jeff tested and confirmed.
