# Session 2026-04-20

## Tasks Completed
- Diagnosed OmniClaw hang: prompt processes to 100% then generates forever / no response
- Ruled out: chat template, reasoning_effort override, logit bias, eject/reload, LM Studio restarts
- Applied correct jinja template (enable_thinking = False) to LM Studio
- Removed request_overrides.enable_thinking from Talos config (lines 237-238)
- Confirmed plain qwen3.5-9b responds fine on same prompt; OmniClaw does not
- Slash wall confirmed as cancelled stream artifact, not the core issue

## What Was NOT Solved
- OmniClaw still not responding. Root cause not confirmed.
- Leading theory: 31k token prompt causing issue specific to OmniClaw model behavior post-sleep resume. Unverified.

## Decisions
- No fabricated claims going forward - flagged by Jeff mid-session

## Open Items
- OmniClaw broken, needs fresh eyes next session
- Check prefill_messages_file (~/.hermes/session_prefill.md) - may be inflating prompt size
- Try sending OmniClaw a minimal prompt directly via curl to isolate whether it's the prompt size or something else

## Next Session
- Start with curl test direct to LM Studio, bypassing Hermes entirely
- If curl works: problem is in what Hermes is sending
- If curl fails: problem is in the model/LM Studio config

## Session 2 (16:49)

### Tasks Completed
- Diagnosed Ares failure: CLōD returning model: None, 0 tokens on all requests
- Switched Ares provider from CLōD to OpenRouter in ~/.hermes/profiles/ares/config.yaml
- Added OpenRouter provider block with shared key from ~/.hermes/.env
- Ares confirmed working after restart

### Decisions
- CLōD left configured but inactive; OpenRouter now primary for Ares

## Session 3 (19:00) — Keeper CDP/IDOR Tooling

### Tasks Completed
- Diagnosed WSL-to-Windows CDP failure: portproxy (svchost PID 4748) squatting on 9222, blocking Brave from binding
- Built `keeper_extract_sessions.py` -- CDP session extractor using Playwright connect_over_cdp + Runtime.evaluate for localStorage
- Built `keeper_idor_test.py` -- cross-account IDOR sweep with localStorage-based auth injection
- Built `keeper_intercept_network.py` -- CDP network interceptor capturing all XHR/fetch with headers and bodies
- Built `keeper_cdp_dump.py` -- diagnostic dumping all storage types (cookies, localStorage, sessionStorage, IndexedDB)
- Discovered Keeper uses localStorage + IndexedDB for auth (not cookies) -- storage_state() returns empty, Runtime.evaluate is the fix
- Discovered Keeper API is protobuf/binary, not REST -- HTTP replay won't work, pivot to Burp/Caido proxy approach
- Wrote `keeper-session-extraction` skill doc: `D:\Ares\knowledge\skills\bug-bounty\keeper-session-extraction\SKILL.md`
- Updated `D:\Shared\bug-bounty-toolkit.md` with new skill entry replacing stale browser-interaction-toolkit reference

### Key Discoveries
- portproxy reset must run as Admin before launching Brave -- otherwise svchost owns the port
- `--remote-allow-origins=*` flag only works if Brave actually binds to the port; if portproxy squats first, flag is silently ignored
- Keeper vault serves SPA HTML for all /vault/* routes -- 200 responses are not JSON API responses
- Auth tokens are in localStorage keys: `vault-device-clone-code`, `vault-platform-device-token-map`, `vault_login`
- IndexedDB databases: `tugipwl6Oitw3m9fIctWZQ` (vault data), `vault-device`

### Open Items
- Keeper IDOR: need Burp/Caido proxy on live session to modify protobuf requests
- Test sharing flow IDOR: share record A1->A2, revoke, check if A2 retains access
